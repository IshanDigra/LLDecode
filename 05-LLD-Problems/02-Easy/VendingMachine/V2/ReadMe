#Requirement
The vending machine should support multiple products with different prices and quantities.
The machine should accept coins and notes of different denominations.
The machine should dispense the selected product and return change if necessary.
The machine should keep track of the available products and their quantities.
The machine should handle multiple transactions concurrently and ensure data consistency.
The machine should provide an interface for restocking products and collecting money.
The machine should handle exceptional scenarios, such as insufficient funds or out-of-stock products.

Bonus: Notify the admin about Low inventory count and restocking the product on time.
Bonus2: Multiple products could be added to cart with varying quantity. - for now assuming only 1 quantity selection

Things to work on: machine.add money delegates to state object which again calls the machine method is very wrong coding.
work arounf this stuff.

Entities:
1) VendingMachine: selectedProd, addedAmount, currentState, InventoryService, Various states
2) Money: type, Amount
3) InventoryService: Map<Product, integer> products, List<Observers>
4) Admin: id, name
5) Product: id, name, type, price

Enums:
1) Money Type: cash, Coin
2) Product Type: buiscuit, schips, bar, drink

Core Functionalities:
1) select a product,
2) reset the product
3) select a product
4) add Money in digital wallet.
5) try make payment
6) add more money make payment
7) machine processes the payment returns the change and finally dispenses the product .
8) restocking products , is available, updating quantity,
states: Idle State, Transaction State, Dispensing State


Critical Sections:
1) Adding Product
2) Updating the inventory
3) making payments
4) dispensing the product.

Exceptions: Out of stock, Insufficient Funds